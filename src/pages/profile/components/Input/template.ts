const template = `
    <input id="{{ id }}" class="input" placeholder="{{ placeholder }}" type="{{ type }}" value="{{ value }}" name="{{ name }}"></input>
    <label for="{{ id }}" class="placeholder">{{ placeholder }}</label>
`;

export default template;
