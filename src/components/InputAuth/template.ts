export const template = `
    <input id="{{ id }}" class="input" placeholder="{{ placeholder }}" type="{{ type }}" name="{{ name }}"></input>
    <label for="{{ id }}" class="placeholder">{{ placeholder }}</label>
`